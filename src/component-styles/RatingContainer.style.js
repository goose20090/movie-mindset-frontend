import styled from "styled-components";
import MovieRatingContainer from "../components/MovieRatingContainer";

export const StyledRatingContainer = styled(MovieRatingContainer)`

display: flexbox;
flex-wrap: wrap;

`