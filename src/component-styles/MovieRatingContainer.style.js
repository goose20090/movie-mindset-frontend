import styled from "styled-components";
import MovieRatingContainer from "../components/movie-page/MovieRatingContainer";

export const StyledMovieRatingContainer = styled(MovieRatingContainer)`

display: flexbox;
flex-wrap: wrap;

`